#include <iostream>
using namespace std;

int main(){
    // Create tgt variable for ptr practice. 
    // Assign the addr of the tgt var to a pointer  (simply create pointer)
    // Assign the addr of the tgt var to a reference variable (Simply create reference)
    // cout tgt, pointer, addrRef 
    // Try dereferencing the pointer to get the value
    // Try dereferencing the addrRef and observe
    // it doesn't error
    // changing tgt val thru ref 
    // changing tgt val thru ptr 
    return 0;
}