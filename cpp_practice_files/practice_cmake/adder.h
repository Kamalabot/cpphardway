#pragma once  //  The #pragma once directive, once present in a file, assures that the file will not be included multiple times in the current project.

namespace kmath {
    int add(int a, int b);
    float add(float a, float b);
}